const display = {
    showTitle: true,
    emptyRowsWhenPaging: false,
    paging: false,
    search: false,
    theme: { },
    toolbar: true,
    padding: "dense",
}

const Defaults = {
    display: display
}

export default Defaults; 